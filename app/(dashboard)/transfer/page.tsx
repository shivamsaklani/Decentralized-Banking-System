import { TransferPage } from "@/components/bank/transfer-page"

export const metadata = {
  title: "Transfer | AstraVault",
  description: "Send and receive funds via QR code.",
}

export default function TransferRoute() {
  return <TransferPage />
}
